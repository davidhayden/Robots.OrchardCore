<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

    <PropertyGroup>
        <PackageId>Robots.OrchardCore</PackageId>
        <Title>Robots.txt Module for Orchard Core</Title>
        <Description>Adds robots.txt functionality.</Description>
        <PackageTags>Robots;OrchardCore</PackageTags>
        <VersionPrefix>1.0.0</VersionPrefix>
        <VersionSuffix>beta3</VersionSuffix>
    </PropertyGroup>

    <!-- Called after so that the <VersionSuffix> is built after the local ones -->
    <Import Project="Common.props"/>

</Project>